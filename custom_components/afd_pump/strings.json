{
  "config": {
    "step": {
      "user": {
        "title": "Connect to AFD Pump Controller",
        "description": "Enter the MQTT topic prefix used by your AFD device (default: AFD)",
        "data": {
          "topic_prefix": "Topic prefix"
        }
      }
    },
    "error": {
      "invalid_prefix": "Invalid topic prefix. Must be non-empty."
    },
    "abort": {
      "already_configured": "Device with this prefix is already configured."
    }
  }
}